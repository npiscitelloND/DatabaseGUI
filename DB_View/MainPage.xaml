<Page
    x:Class="DB_View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DB_View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Margin="5">

    <StackPanel Name="background" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Border Name="header_border" BorderBrush="Black" BorderThickness="1" Margin="0">
            <RelativePanel Name="header">
                <TextBlock Name="name" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Style="{StaticResource TitleTextBlockStyle}" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" Margin="5,5,5,0" FontWeight="Bold"/>
                <TextBlock Name="price" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Style="{StaticResource TitleTextBlockStyle}" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.RightOf="" TextAlignment="Right" Margin="5,5,5,0"/>
                <TextBlock Name="description" HorizontalAlignment="Left" VerticalAlignment="Top" RelativePanel.Below="name" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" Margin="5" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}"/>
            </RelativePanel>
        </Border>

        <Border Name="images_border" BorderBrush="Black" BorderThickness="1" Margin="0">
            <RelativePanel Name="images">
                
            </RelativePanel>
        </Border>
        
        <Border Name="details_border" BorderBrush="Black" BorderThickness="1" Margin="0">
            <RelativePanel Name="details">
                
            </RelativePanel>
        </Border>
        
        <Border Name="procurement_border" BorderBrush="Black" BorderThickness="1" Margin="0">
            <RelativePanel Name="procurement">
                
            </RelativePanel>
        </Border>
    </StackPanel>
</Page>
